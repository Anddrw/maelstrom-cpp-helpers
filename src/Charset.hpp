#pragma once
#include "Header.hpp"

class maelstrom::charset
{
public:
	static std::string 
	get(const string& _input)
	{

	}

	static std::string
	convert_to(const string& _input, const string& _charset)
	{

	}

	static bool
	is_charset(const string& _input, const string& _charset)
	{
		return true;
	}
};
